import 'package:flutter/material.dart';

class ReservationCardMobile extends StatelessWidget {
  const ReservationCardMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
